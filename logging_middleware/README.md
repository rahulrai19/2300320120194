# Logging Middleware

Reusable logging middleware package.

## Function

Log(stack, level, package, message)

## Example

Log(
  "backend",
  "info",
  "controller",
  "Fetching notifications"
)


## Supported Stacks

- backend
- frontend